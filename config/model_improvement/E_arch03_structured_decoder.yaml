base_config: ../top_reconstruction_config.yaml

# Mask losses plus soft W-in-parent-top consistency.
tasks:
  mask_consistency:
    loss_weight: 0.1
    margin: 0.0
    layer_weight_strategy: final_only

model_artefacts:
  log_dir: model_improvement/E_arch03_structured_decoder
