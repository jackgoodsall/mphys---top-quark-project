base_config: ../top_reconstruction_config.yaml

# E4 capacity variant with mask losses plus soft W-in-parent-top consistency.
tasks:
  mask:
    mask_embed_head: true
  mask_W:
    mask_embed_head: true
  mask_consistency:
    loss_weight: 0.1
    margin: 0.0
    layer_weight_strategy: final_only

model_parameters:
  interaction_embedder:
    hidden_layers: [64, 64, 64]
    output_size: 8
  transformer:
    n_heads: 8
    mask_logit_scale: true

model_artefacts:
  log_dir: model_improvement/E_arch04_combined
